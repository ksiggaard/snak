/** Spacer that preserves the top padding above the sidebar's first control
 *  now that the chrome (title, toggle, menu) has moved into TitleBar. */
export function SidebarHeader(_props: { onClose: () => void }) {
  return null;
}
