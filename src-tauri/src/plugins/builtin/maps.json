{
  "id": "com.snak.maps",
  "name": "Maps",
  "version": "1.0.0",
  "category": "renderer",
  "apiVersion": 1,
  "description": "Renders ```map (and ```geojson) fenced code blocks as interactive OpenStreetMap maps with markers and road-snapped routes. No API key. Disabled by default; enable it to draw the map (disabled keeps the raw GeoJSON).",
  "author": "snak",
  "enabledByDefault": false,
  "contributes": {
    "language": "map"
  }
}
