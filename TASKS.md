# TASKS

Tasks now live in [`docs/tasks/`](docs/tasks/) — one file per task,
foldered by status (`todo/` · `in-progress/` · `done/`). The 64 historical tasks (T1–T64,
all done) were migrated there out of this file.

- **Start:** [`docs/tasks/README.md`](docs/tasks/README.md) — the task workflow.
- **A specific task:** `docs/tasks/<status>/T<NN>-*.md` (e.g. T5 →
  [`docs/tasks/done/T05-kde-linux-packaging-app-branding.md`](docs/tasks/done/T05-kde-linux-packaging-app-branding.md)).
- **Architecture & conventions:** [`AGENTS.md`](AGENTS.md).
